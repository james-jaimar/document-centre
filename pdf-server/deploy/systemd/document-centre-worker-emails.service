[Unit]
Description=DISABLED - Document Centre legacy VPS email worker

[Service]
Type=oneshot
ExecStart=/bin/sh -c 'echo "document-centre-worker-emails.service is disabled: email sending is Cloud Run only (pdf-worker-emails)." >&2; exit 78'

[Install]
WantedBy=multi-user.target
