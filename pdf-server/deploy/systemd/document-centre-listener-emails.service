[Unit]
Description=DISABLED - Document Centre legacy VPS email listener

[Service]
Type=oneshot
ExecStart=/bin/sh -c 'echo "document-centre-listener-emails.service is disabled: email notifications route to Cloud Run only." >&2; exit 78'

[Install]
WantedBy=multi-user.target
